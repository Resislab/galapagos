export type LOGIN_TYPE = {
    loginPage: {
        organizationName: string
        username: string
        password: string
        logInButton: {
            submitting: string
            toSubmit: string
        }
    }
}

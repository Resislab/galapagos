export type LOGIN_TYPE = {
    loginPage: {
        organizationName: string
        email: string
        password: string
        alternative: string
        createAccount: string
        logInButton: {
            submitting: string
            toSubmit: string
        }
    }
}

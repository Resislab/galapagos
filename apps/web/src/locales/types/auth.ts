export type AUTH_TYPE = {
    signInPage: {
        email: string
        password: string
        alternative: string
        createAccount: string
        logInButton: {
            submitting: string
            toSubmit: string
        }
    }
}
